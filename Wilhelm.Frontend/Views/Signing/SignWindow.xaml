<Window x:Class="Wilhelm.Frontend.Views.Signing.SignWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wilhelm.Frontend.Views.Signing"
        xmlns:viewModels="clr-namespace:Wilhelm.Frontend.ViewModels.Signing"
        mc:Ignorable="d"
        Title="SignWindow" Height="300" Width="300">

    <Window.DataContext>
        <viewModels:SignViewModel />
    </Window.DataContext>


    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModels:SignInViewModel}">
            <local:SignInView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:SignUpViewModel}">
            <local:SignUpView/>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <ContentControl Content="{Binding SelectedViewModel}"/>
    </DockPanel>
</Window>
